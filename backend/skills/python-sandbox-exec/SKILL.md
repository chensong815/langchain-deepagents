---
name: python-sandbox-exec
description: 在需要精确计算、数据处理、代码验证或生成临时文件时，使用会话级 Python 工作区工具执行代码并基于结果回答。
path: /skills/python-sandbox-exec/SKILL.md
allowed-tools: run_python_code
triggers:
  - python
  - 计算
  - 验证
required-slots: []
output-contract: 已执行结果、关键输出、失败点或下一步建议
---

# Python Sandbox Exec

## 适用场景

- 用户明确要求“运行 Python / 执行代码 / 验证结果 / 算一下”。
- 问题需要精确计算、数据清洗、格式转换、批量处理或解析结构化文本。
- 需要通过实际运行代码验证某个结论，而不是仅凭推断回答。
- 需要生成临时文件或临时脚本。

## 工作流程

1. 如果直接回答即可满足需求，不要为了展示能力而调用工具。
2. 需要运行代码时，优先使用 `run_python_code`，并在代码中显式 `print(...)` 输出关键结果。
3. 若问题依赖第三方库，先判断部署容器是否已预装；未预装时不要假设可以在运行时安装。
4. 代码应尽量短、小、可复现，优先使用标准库或容器内已提供的依赖。
5. 若需要写入临时文件，只写到当前会话 sandbox 的工作目录，不要假设宿主项目目录可写。
6. 回答时先给执行结果，再给简短解释；不要把未执行的推断表述成“已验证”。

## 结果约束

- 只有在工具返回 `ok=true` 时，才声称代码已成功运行。
- 如果执行失败，明确说明失败点，并基于 `stderr`/`stdout` 给出下一步处理。
- 不要凭心算替代应当通过代码验证的计算任务。
